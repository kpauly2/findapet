package tech.pauly.findapet.data.models;

public enum PhotoSize {
    x,      //large (max 500x500)
    t,      //thumbnail (max 50 pixels high)
    pn,     //petnote (max 300x250)
    pnt,    //petnote thumbnail (max 60 pixels wide)
    fpm     //featured pet module (max 95 pixels wide)
}
